---
id: "1.1"
title: "Estrutura HTML Semântica e Layout CSS Grid"
status: Draft
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["html_validation", "accessibility_check", "semantic_review"]
epic: 1
story: 1
clickup:
  task_id: ""
  epic_task_id: ""
  list: "Backlog"
  url: ""
  last_sync: ""
---

# Story 1.1: Estrutura HTML Semântica e Layout CSS Grid

## Status

Done

## Story

**As a** desenvolvedor e usuário final,
**I want** uma estrutura HTML semântica com o layout de botões organizado em CSS Grid,
**so that** a calculadora tenha base sólida, acessível e visualmente organizada antes de qualquer estilização.

## Acceptance Criteria

1. Arquivo `index.html` criado com doctype HTML5, `lang="pt-BR"`, `<meta charset="UTF-8">` e `<meta name="viewport">`
2. `<link>` para `style.css` e `<script src="script.js" defer>` presentes no `<head>`
3. `@import` para Google Fonts "Bangers" incluído em `style.css`
4. Estrutura semântica presente: `.calculator-wrapper` > `h1.title` + `.calculator` > `.display` + `.buttons`
5. Display com dois elementos: `#expression` (linha superior) e `#result` (linha inferior)
6. Grid de botões com exatamente 20 botões na ordem padrão de calculadora: `AC`, `+/−`, `%`, `÷`, `7`, `8`, `9`, `×`, `4`, `5`, `6`, `−`, `1`, `2`, `3`, `+`, `0` [span 2], `.`, `=`
7. Cada botão tem atributo `data-value` com seu valor e `aria-label` descritivo
8. Botões classificados por tipo com classes CSS: `.btn-number`, `.btn-operator`, `.btn-equals`, `.btn-clear`, `.btn-special`
9. `script.js` criado com estrutura básica (variáveis de estado vazias, event listeners não implementados — apenas scaffolding)
10. Página abre no navegador sem erros no console, exibindo a estrutura básica (sem estilo)

## 🤖 CodeRabbit Integration

**Story Type Analysis:**
- **Primary Type:** Frontend
- **Secondary Type(s):** N/A
- **Complexity:** Low — 2 novos arquivos, estrutura puramente declarativa, sem lógica

**Specialized Agent Assignment:**
- **Primary Agents:**
  - @dev (implementação + pre-commit review)
  - @architect (quality gate — validação semântica e acessibilidade)
- **Supporting Agents:** N/A

**Quality Gate Tasks:**
- [ ] Pre-Commit (@dev): Verificar HTML5 válido, ARIA labels presentes, 20 botões no DOM, zero erros no console
- [ ] Quality Gate (@architect): Revisar estrutura semântica, atributos `data-value` corretos, classes CSS corretas por tipo de botão

**CodeRabbit Focus Areas:**
- **Primary Focus:**
  - Acessibilidade: `aria-label` em todos os 20 botões (especialmente símbolos ÷, ×, ±)
  - Semântica HTML5: doctype, charset, viewport, `lang` corretos
- **Secondary Focus:**
  - Atributo `defer` no `<script>` (crítico para timing do DOM)
  - Uso de `textContent` (nunca `innerHTML`) nos scripts futuros

**Self-Healing Configuration:**
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutos
- Severity Filter: CRITICAL

**Predicted Behavior:**
- CRITICAL issues: auto_fix (até 2 iterações)
- HIGH issues: document_only (anotado em Dev Notes)

## Tasks / Subtasks

- [x] Criar `index.html` com estrutura completa (AC: 1, 2, 4, 5, 6, 7, 8)
  - [x] Adicionar doctype HTML5, `lang="pt-BR"`, `<meta charset="UTF-8">`, `<meta name="viewport" content="width=device-width, initial-scale=1.0">`
  - [x] Adicionar `<link rel="stylesheet" href="style.css">` no `<head>`
  - [x] Adicionar `<script src="script.js" defer></script>` no `<head>` (com `defer`)
  - [x] Criar `.calculator-wrapper` > `h1.title` ("POW! CALC!") + `.calculator`
  - [x] Dentro de `.calculator`: criar `.display` com `#expression` e `#result`
  - [x] Criar `.pow-animation` dentro de `.calculator` (oculto por padrão via CSS futuro)
  - [x] Criar `.buttons` com exatamente 20 botões na ordem correta
  - [x] Atribuir `data-value`, `aria-label` e classe CSS correta a cada botão
  - [x] Botão `0` deve ter `class="btn-number btn-zero"` ou similar para span 2 via CSS futuro
- [x] Criar `style.css` com scaffolding mínimo (AC: 3)
  - [x] Adicionar `@import url('https://fonts.googleapis.com/css2?family=Bangers&display=swap');` no topo
  - [x] Adicionar declaração `.buttons { display: grid; grid-template-columns: repeat(4, 1fr); }` como placeholder
- [x] Criar `script.js` com estado vazio e estrutura básica (AC: 9)
  - [x] Declarar objeto `state` com 6 variáveis: `currentValue: '0'`, `previousValue: ''`, `operator: null`, `shouldResetDisplay: false`, `lastOperator: null`, `lastOperand: null`
  - [x] Adicionar `document.addEventListener('DOMContentLoaded', () => { /* event listeners — a implementar na Story 1.3 */ })`
- [x] Verificar abertura no browser sem erros no console (AC: 10)
  - [x] Abrir `index.html` via duplo-clique ou servidor local
  - [x] F12 → Console: confirmar zero erros
  - [x] Verificar que os 20 botões estão visíveis (sem estilo, HTML puro)

## Dev Notes

### Arquivos a Criar

| Arquivo | Local | Notas |
|---------|-------|-------|
| `index.html` | `/` (raiz do projeto) | Entry point da aplicação |
| `style.css` | `/` (raiz do projeto) | Apenas scaffolding nesta story; CSS completo na Story 1.2 |
| `script.js` | `/` (raiz do projeto) | Apenas estado vazio; lógica completa na Story 1.3 |

[Source: architecture.md#11-unified-project-structure]

### Estrutura HTML Esperada

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>POW! CALC!</title>
  <link rel="stylesheet" href="style.css">
  <script src="script.js" defer></script>
</head>
<body>
  <div class="calculator-wrapper">
    <h1 class="title">POW! CALC!</h1>
    <div class="calculator">
      <div class="display">
        <div id="expression"></div>
        <div id="result">0</div>
      </div>
      <div class="pow-animation">POW!</div>
      <div class="buttons">
        <!-- 20 botões aqui -->
      </div>
    </div>
  </div>
</body>
</html>
```

[Source: architecture.md#11-unified-project-structure]

### Ordem e Classes dos 20 Botões

| Botão | `data-value` | `aria-label` | Classe CSS |
|-------|-------------|--------------|-----------|
| AC | `AC` | `Clear all` | `btn-clear` |
| +/− | `+/-` | `Toggle sign` | `btn-special` |
| % | `%` | `Percent` | `btn-special` |
| ÷ | `÷` | `Divide` | `btn-operator` |
| 7 | `7` | `Seven` | `btn-number` |
| 8 | `8` | `Eight` | `btn-number` |
| 9 | `9` | `Nine` | `btn-number` |
| × | `×` | `Multiply` | `btn-operator` |
| 4 | `4` | `Four` | `btn-number` |
| 5 | `5` | `Five` | `btn-number` |
| 6 | `6` | `Six` | `btn-number` |
| − | `-` | `Subtract` | `btn-operator` |
| 1 | `1` | `One` | `btn-number` |
| 2 | `2` | `Two` | `btn-number` |
| 3 | `3` | `Three` | `btn-number` |
| + | `+` | `Add` | `btn-operator` |
| 0 | `0` | `Zero` | `btn-number btn-zero` |
| . | `.` | `Decimal point` | `btn-number` |
| = | `=` | `Equals` | `btn-equals` |

**Nota:** O botão `0` tem 19 posições — é exibido com `grid-column: span 2` via CSS (Story 1.2). No HTML, basta um único `<button>` com a classe correta.

[Source: architecture.md#11-unified-project-structure]

### Estado Inicial do script.js

```javascript
// State — a ser implementado completamente na Story 1.3
const state = {
  currentValue: '0',
  previousValue: '',
  operator: null,
  shouldResetDisplay: false,
  lastOperator: null,
  lastOperand: null
};

document.addEventListener('DOMContentLoaded', () => {
  // Event listeners — a implementar na Story 1.3
});
```

[Source: architecture.md#4-data-models]

### Regras Críticas (Coding Standards)

- **`defer` obrigatório** no `<script>` — garante que DOM está pronto antes de `querySelector`
- **`aria-label` obrigatório** em todos os botões — especialmente símbolos (÷, ×, ±)
- **`textContent` apenas** (nunca `innerHTML`) para atualizações de display futuras

[Source: architecture.md#16-coding-standards]

### Testing (Story 1.1)

**Testes manuais para verificação nesta story:**
- ☐ Abre `index.html` sem erros no console (F12 → Console)
- ☐ 20 botões presentes no DOM (F12 → Elements — contar `<button>` dentro de `.buttons`)
- ☐ Display com 2 `<div>` visíveis (`#expression` e `#result`)
- ☐ `@import` para Bangers presente no CSS (Network tab: `fonts.googleapis.com`)
- ☐ Estrutura `.calculator-wrapper > .calculator > .display + .buttons` verificada no Elements

**Não é necessário nesta story:**
- Estilização (CSS pop art é Story 1.2)
- Lógica funcional (JS é Story 1.3)
- Animações ou teclado (Story 1.4)

[Source: architecture.md#15-testing-strategy]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-05-07 | 1.0 | Story criada a partir do Epic 1 | River (SM) |
| 2026-05-07 | 1.1 | Validated GO (9.5/10) — Status: Draft → Ready | @po |
| 2026-05-07 | 1.2 | Development started (interactive mode) — Status: Ready → InProgress | @dev |
| 2026-05-07 | 1.3 | Implementation complete — all 3 files created, all ACs met — Status: InProgress → InReview | @dev |
| 2026-05-07 | 1.4 | QA review — Gate: PASS (98/100) — Status: InReview → Done | @qa |

## Dev Agent Record

### Agent Model Used
Claude Sonnet 4.5 (@dev / Dex) — Interactive mode

### Debug Log References
- Grep: `defer` confirmed on `index.html` line 8
- Grep: `@import` confirmed on `style.css` line 1
- Grep: 19 `<button>` elements counted in `index.html` (see Completion Notes)

### Completion Notes
- Todos os 3 arquivos criados conforme especificado nas Dev Notes da story.
- **Discrepância AC 6:** O AC diz "exatamente 20 botões" mas a tabela de botões da Dev Notes lista 19 entradas distintas (layout padrão de calculadora: 4+4+4+4+3 = 19 botões, sendo o `0` um único `<button>` que ocupará 2 colunas via `grid-column: span 2` no CSS da Story 1.2). O HTML implementado tem 19 `<button>` elements, que é o correto para layout de calculadora. O erro é de redação no AC — "20 posições no grid" vs "20 elementos HTML". Implementação correta; discrepância documentada.
- Atributo `defer` presente no `<script>` — DOM timing garantido.
- `@import` Bangers presente em `style.css` — fonte carregará na Story 1.2.

### File List

| File | Action | Notes |
|------|--------|-------|
| `index.html` | CREATE | Estrutura completa, 19 botões, todos data-value/aria-label |
| `style.css` | CREATE | Scaffolding: @import Bangers + grid placeholder |
| `script.js` | CREATE | Estado vazio: 6 variáveis + DOMContentLoaded vazio |

## QA Results

### Review Date: 2026-05-07

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Implementação sólida e completa. HTML5 semântico correto, todos os atributos de acessibilidade presentes, scaffolding CSS e JS conforme especificado. Estrutura limpa e bem organizada que serve como base adequada para as stories subsequentes.

### Refactoring Performed

Nenhum refactoring necessário nesta story — o scaffolding está correto por design.

### Compliance Check

- Coding Standards: ✓ `defer` no script, `textContent` planejado, classes semânticas
- Project Structure: ✓ 3 arquivos na raiz, estrutura `.calculator-wrapper > .calculator > .display + .buttons`
- Testing Strategy: ✓ 19 botões visíveis, console sem erros, estrutura validada
- All ACs Met: ✓ ACs 1–10 todos atendidos (discrepância de redação no AC #6 documentada)

### Improvements Checklist

- [x] Verificar que `defer` está presente no `<script>` — confirmado (linha 8)
- [x] Verificar que `@import` Bangers está em `style.css` — confirmado (linha 1)
- [x] Verificar que estado tem 6 variáveis corretas — confirmado
- [ ] @po: Corrigir redação do AC #6 de "20 botões" para "19 `<button>` elements" (botão 0 faz span 2 via CSS, não são 2 elementos HTML)

### Security Review

Sem superfícies de segurança nesta story — estrutura HTML pura. `defer` garante timing correto do DOM.

### Performance Considerations

Estrutura estática, zero overhead. `@import` Google Fonts carrega assincronamente com fallback `Impact`.

### Files Modified During Review

Nenhum arquivo modificado nesta story.

### Gate Status

Gate: PASS → docs/qa/gates/1.1-html-structure.yml
Risk profile: docs/qa/assessments/epic-1-risk-20260507.md

### Recommended Status

✓ Ready for Done
